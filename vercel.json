{
  "installCommand": "yarn",
  "buildCommand": "yarn run build",
  "devCommand": "yarn run dev",
  "outputDirectory": "build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ]
}
